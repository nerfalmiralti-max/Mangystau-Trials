# Local development SQLite database.
# For Vercel production, use a cloud database and set DATABASE_URL in Vercel settings.
DATABASE_URL="file:./dev.db"

# Required for secure login cookies in production.
AUTH_SECRET="replace-with-a-long-random-secret"

# Use "auto" to call OpenAI when OPENAI_API_KEY is set, otherwise use offline guidance.
# Valid values: auto, openai, ollama, offline.
AI_PROVIDER="auto"
AI_TIMEOUT_MS="15000"

# Optional Vercel AI mode.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4.1-mini"

# Local-only Ollama mode. Do not use this on Vercel.
OLLAMA_API_URL="http://127.0.0.1:11434"
OLLAMA_MODEL="llama3.2:3b"

# Contact form SMTP settings. Set these in .env locally and in Vercel project settings.
CONTACT_EMAIL="hello@example.com"
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user@example.com"
SMTP_PASS="replace-with-smtp-password"
SMTP_SECURE="false"
SMTP_FROM="smtp-user@example.com"
